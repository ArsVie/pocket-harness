# Copy to `.env` and fill in. `.env` is gitignored; this file is not, so it must never hold a real key.
#
# Used by the debug-only seeding path (`DebugEnvBootstrap`, gated on BuildConfig.DEBUG) and by the
# helper scripts. In a release build the key is entered in Settings and stored in the Android Keystore.
API_KEY=
