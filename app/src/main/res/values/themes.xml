<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws everything; the platform theme only needs to be a non-action-bar shell. -->
    <style name="Theme.PocketHarness" parent="@android:style/Theme.Material.Light.NoActionBar" />
</resources>
