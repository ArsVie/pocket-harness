<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- extractNativeLibs=true (equivalent): declared in build.gradle.kts as
         packaging.jniLibs.useLegacyPackaging = true, because AGP 9 rejects the manifest attribute
         outright (issue #149770867). The shipped userland (static aarch64 busybox) must be
         extracted and executable from the APK's native library dir (ADR-001 / ENVIRONMENT.md). -->
    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PocketHarness">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PocketHarness">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
