.gradle/
build/
.kotlin/
local.properties
.idea/
*.iml
captures/
.externalNativeBuild/
.cxx/

# Secrets: real API keys live here, never in the repo (see AGENTS.md).
.env
*.env
!.env.example

# Working notes — planning lives on disk for the agents, never in the public tree.
planning/
